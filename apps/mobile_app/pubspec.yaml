name: mobile_app
description: offline_School mobile application for teacher attendance workflows
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  desktop_app:
    path: ../desktop_app

dev_dependencies:
  flutter_test:
    sdk: flutter
  dio: ^5.4.3
  drift: ^2.18.0
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
